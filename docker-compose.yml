services:
  employee-api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=default
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8080/api/employees"]
      interval: 10s
      timeout: 5s
      retries: 3
